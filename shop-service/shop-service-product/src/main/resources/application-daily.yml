server:
  port: 7003
spring:
  application:
    name: shop-eureka
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
  server:
    enable-self-preservation: false