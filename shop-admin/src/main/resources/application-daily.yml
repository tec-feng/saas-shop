server:
  port: 7003
spring:
  application:
    name: shop-admin
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: false
    register-with-eureka: false
  server:
    enable-self-preservation: false